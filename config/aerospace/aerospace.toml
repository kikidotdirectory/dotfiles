# Config version for compatibility and deprecations
# Fallback value (if you omit the key): config-version = 1
config-version = 2

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config version is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = ["1", "2", "3" ]

# A callback that runs every time binding mode changes
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# See: https://nikitabobko.github.io/AeroSpace/commands#mode
on-mode-changed = ['exec-and-forget sketchybar --trigger aerospace_mode_changed']

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands

# Run Sketchybar together with AeroSpace
# sketchybar has a built-in detection of already running process,
# so it won't be run twice on AeroSpace restart
after-startup-command = ['exec-and-forget sketchybar']

# Notify Sketchybar about workspace change
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

on-window-detected = [
  # Refreshes sketchybar's space icons on every new window, regardless of
  # which app it belongs to. check-further-callbacks lets the app-specific
  # move-node-to-workspace rules below still run normally after this.
  {
    check-further-callbacks = true,
    run = 'exec-and-forget sketchybar --trigger aerospace_workspace_change',
  },
  {
    if = 'test %{app-bundle-id} = com.apple.finder',
    run = ['move-node-to-workspace F'],
  },
  {
    if = 'test %{app-bundle-id} = com.flexibits.fantastical2.mac',
    run = 'move-node-to-workspace L'
  },
  {
    if = 'test %{app-bundle-id} = com.anthropic.claudefordesktop',
    run = 'move-node-to-workspace C'
  },
  {
    if = 'test %{app-bundle-id} = com.apple.MobileSMS',
    run = 'move-node-to-workspace M',
  },
  {
    if = 'test %{app-bundle-id} = md.obsidian',
    run = 'move-node-to-workspace O',
  },
  {
    if = 'test %{app-bundle-id} = com.figma.Desktop',
    run = 'move-node-to-workspace D',
  },
  {
    if = 'test %{app-bundle-id} = com.apple.Safari',
    run = 'move-node-to-workspace S',
  },
  {
    if = 'test %{app-bundle-id} = com.apple.mail',
    run = 'move-node-to-workspace E',
  },
  {
    if = 'test %{app-bundle-id} = co.brushedtype.doppler-macos',
    run = 'move-node-to-workspace U',
  },
  {
    if = 'test %{app-bundle-id} = com.apple.ActivityMonitor',
    run = 'move-node-to-workspace A',
  },
]

[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
    inner.horizontal = 4
    inner.vertical =   4
    outer.left =       8
    outer.bottom =     8
    # The main monitor is where Sketchybar's bar renders (see `display = "main"`
    # in config/sketchybar/init.lua). It needs extra top clearance for the bar
    # itself; the built-in display's notch normally provides this space for
    # free, but the extended display has no notch, so give the main monitor
    # explicit room whichever physical display it happens to be.
    outer.top =        [{ monitor.main = 44 }, 8]
    outer.right =      8

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    alt-slash       = 'layout tiles horizontal vertical'
    alt-comma       = 'layout accordion horizontal vertical'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    alt-shift-h           = 'focus left'
    alt-shift-j           = 'focus down'
    alt-shift-k           = 'focus up'
    alt-shift-l           = 'focus right'

    alt-shift-left        = 'focus left'
    alt-shift-down        = 'focus down'
    alt-shift-up          = 'focus up'
    alt-shift-right       = 'focus right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    alt-ctrl-h     = 'move left'
    alt-ctrl-j     = 'move down'
    alt-ctrl-k     = 'move up'
    alt-ctrl-l     = 'move right'

    alt-ctrl-left  = 'move left'
    alt-ctrl-down  = 'move down'
    alt-ctrl-up    = 'move up'
    alt-ctrl-right = 'move right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    alt-1           = 'workspace 1'
    alt-2           = 'workspace 2'
    alt-3           = 'workspace 3'
    alt-4           = 'workspace 4'
    alt-5           = 'workspace 5'
    alt-6           = 'workspace 6'
    alt-7           = 'workspace 7'
    alt-8           = 'workspace 8'
    alt-9           = 'workspace 9'
    # move to other monitor
    alt-0           = 'workspace 10'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    alt-shift-1     = 'move-node-to-workspace 1'
    alt-shift-2     = 'move-node-to-workspace 2'
    alt-shift-3     = 'move-node-to-workspace 3'
    alt-shift-4     = 'move-node-to-workspace 4'
    alt-shift-5     = 'move-node-to-workspace 5'
    alt-shift-6     = 'move-node-to-workspace 6'
    alt-shift-7     = 'move-node-to-workspace 7'
    alt-shift-8     = 'move-node-to-workspace 8'
    alt-shift-9     = 'move-node-to-workspace 9'
    # move to other monitor
    alt-shift-0     = 'move-node-to-workspace 10'

    # App launcher workspaces
    # See: https://willschenk.com/howto/2025/how_to_turn_aerospace_into_an_application_launcher/
    alt-c           = ['exec-and-forget open -a Claude',             'workspace C']
    alt-f           = ['exec-and-forget open -a Finder',             'workspace F']
    alt-l           = ['exec-and-forget open -a Fantastical',        'workspace L']
    alt-o           = ['exec-and-forget open -a Obsidian',           'workspace O']
    alt-d           = ['exec-and-forget open -a Figma',              'workspace D']
    alt-m           = ['exec-and-forget open -a Messages',           'workspace M']
    alt-s           = ['exec-and-forget open -a Safari',             'workspace S']
    alt-e           = ['exec-and-forget open -a Mail',               'workspace E']
    alt-u           = ['exec-and-forget open -a Doppler',            'workspace U']
    alt-a           = ['exec-and-forget open -a "Activity Monitor"', 'workspace A']

    # Move focused window to an app's workspace (mirrors alt-shift-1..9 for numbered workspaces)
    alt-shift-c     = 'move-node-to-workspace C'
    alt-shift-f     = 'move-node-to-workspace F'
    alt-shift-o     = 'move-node-to-workspace O'
    alt-shift-d     = 'move-node-to-workspace D'
    alt-shift-s     = 'move-node-to-workspace S'
    alt-shift-e     = 'move-node-to-workspace E'
    alt-shift-u     = 'move-node-to-workspace U'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    alt-tab         = 'list-workspaces --monitor focused --empty no | workspace --stdin next'
    alt-shift-tab   = 'list-workspaces --monitor focused --empty no | workspace --stdin prev'

    # Mode bindings
    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    alt-shift-semicolon = 'mode service'
    alt-shift-p     = 'mode passthrough'

[mode.passthrough.binding]
    esc             = 'mode main'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc             = ['reload-config',                 'mode main']
    r               = ['flatten-workspace-tree',        'mode main'] # reset layout
    f               = ['layout floating tiling',        'mode main'] # Toggle between floating and tiling layout
    backspace       = ['close-all-windows-but-current', 'mode main']

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
    #s = ['layout sticky tiling', 'mode main']

    alt-shift-h     = ['join-with left' , 'mode main']
    alt-shift-j     = ['join-with down' , 'mode main']
    alt-shift-k     = ['join-with up'   , 'mode main']
    alt-shift-l     = ['join-with right', 'mode main']

    alt-shift-left  = ['join-with left' , 'mode main']
    alt-shift-down  = ['join-with down' , 'mode main']
    alt-shift-up    = ['join-with up'   , 'mode main']
    alt-shift-right = ['join-with right', 'mode main']
